name: CI

on:
  push:
    branches:
      - Andrew
  pull_request:
    branches:
      - Andrew

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Install dependencies
        run: npm install
      - name: Run Build
        run: npm run build
      - name: Test
        run: npm run test